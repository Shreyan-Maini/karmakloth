## Gallery photos

Drop your images into this folder and they will automatically appear on the Gallery page at `/gallery`.

- **Supported formats**: `.jpg`, `.jpeg`, `.png`, `.webp`
- **Naming tip**: use simple names like `drive-01.jpg`, `drive-02.jpg` so they sort nicely.

